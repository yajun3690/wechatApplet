<template name="starTemplate">
  <view class="star-wrap">
    <block wx:for="{{stars}}" wx:for-item="num" wx:key="index">
    <image src='{{num == 1 ? "/images/icon/shoucang2.png" : "/images/icon/shoucang.png"}}'></image>
    </block>
    <text class="score">{{score}}</text>
  </view>
</template> 